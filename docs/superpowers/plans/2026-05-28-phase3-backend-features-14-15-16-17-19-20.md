# Phase 3 Backend — Features 14/15/16/17/19/20 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implémenter les features 14 (pré-dunning CB), 15 (churn prédictif), 16 (WhatsApp Business), 17 (rapports vidéo IA), 19 (coaching élève), 20 (SMS Twilio) dans le backend Node.js/Express/TypeScript existant.

**Architecture:**
- Migrations 017-019 : table `student_profiles` (card_exp, last_lms_activity, phone), champs WhatsApp sur `clients`, colonne `channel` sur `email_tracking`
- Trois nouveaux services : `sms.ts` (Twilio REST), `whatsapp.ts` (Meta Graph API v19.0), `videoreport.ts` (ElevenLabs fetch + @napi-rs/canvas + ffmpeg)
- Un helper `utils/sendMultiChannel.ts` injecté dans les 6 auto-send existants (webhooks + cron)
- Quatre nouvelles fonctions cron daily (08h UTC) : `runPredunning`, `runChurnDetection`, `runStudentCoaching`, `sendVideoReport`
- Les élèves n'ont pas de table dédiée — ils sont dérivés de `pending_tasks.context_json` (clé `customer_email`). `student_profiles` est le stockage persistant des données per-élève.

**Tech Stack:** Node.js 18/Express/TypeScript, Supabase, Stripe SDK (déjà installé), Resend, `@napi-rs/canvas`, `fluent-ffmpeg`, `ffmpeg-static`, `twilio`

---

## Fichiers créés / modifiés

### Créés
- `supabase/migrations/017_student_profiles.sql`
- `supabase/migrations/018_whatsapp_fields.sql`
- `supabase/migrations/019_email_tracking_channel.sql`
- `backend/src/services/sms.ts`
- `backend/src/services/whatsapp.ts`
- `backend/src/services/videoreport.ts`
- `backend/src/utils/sendMultiChannel.ts`

### Modifiés
- `backend/package.json` — @napi-rs/canvas, fluent-ffmpeg, ffmpeg-static, twilio
- `backend/src/schemas/client.ts` — 4 nouveaux config_types
- `backend/src/utils/tracking.ts` — param `channel` optionnel
- `backend/src/routes/webhooks.ts` — customer.updated, payment_method.updated, phone extraction, sendEmailWithChannels
- `backend/src/routes/clientAuth.ts` — WhatsApp endpoints, /client/me
- `backend/src/cron.ts` — 4 nouvelles fonctions, injection sendEmailWithChannels dans les flows existants
- `backend/src/index.ts` — registration des crons + env

---

## Task 1 : Migrations SQL (017 → 019)

**Files:**
- Create: `supabase/migrations/017_student_profiles.sql`
- Create: `supabase/migrations/018_whatsapp_fields.sql`
- Create: `supabase/migrations/019_email_tracking_channel.sql`

- [ ] **Créer 017_student_profiles.sql**

```sql
-- 017_student_profiles.sql
CREATE TABLE student_profiles (
  client_id UUID REFERENCES clients(id) ON DELETE CASCADE NOT NULL,
  email TEXT NOT NULL,
  card_exp TIMESTAMPTZ,
  last_lms_activity TIMESTAMPTZ,
  phone TEXT,
  updated_at TIMESTAMPTZ DEFAULT now(),
  PRIMARY KEY (client_id, email)
);

CREATE INDEX idx_student_profiles_card_exp
  ON student_profiles(client_id, card_exp)
  WHERE card_exp IS NOT NULL;

CREATE INDEX idx_student_profiles_lms
  ON student_profiles(client_id, last_lms_activity)
  WHERE last_lms_activity IS NOT NULL;
```

- [ ] **Créer 018_whatsapp_fields.sql**

```sql
-- 018_whatsapp_fields.sql
ALTER TABLE clients
  ADD COLUMN whatsapp_phone_number_id TEXT,
  ADD COLUMN whatsapp_access_token    TEXT,
  ADD COLUMN whatsapp_active          BOOLEAN NOT NULL DEFAULT false;
```

- [ ] **Créer 019_email_tracking_channel.sql**

```sql
-- 019_email_tracking_channel.sql
ALTER TABLE email_tracking
  ADD COLUMN channel TEXT NOT NULL DEFAULT 'email';

CREATE INDEX idx_email_tracking_channel
  ON email_tracking(client_id, channel);
```

- [ ] **Appliquer dans Supabase SQL Editor dans l'ordre 017 → 018 → 019**

- [ ] **Créer le bucket Supabase Storage `rapports-video`**

Dans le dashboard Supabase → Storage → New bucket, nom `rapports-video`, accès private.

- [ ] **Commit**

```bash
git add supabase/migrations/
git commit -m "feat(db): migrations 017-019 — student_profiles, whatsapp fields, email_tracking channel"
```

---

## Task 2 : Dépendances npm

**Files:**
- Modify: `backend/package.json`

- [ ] **Installer les packages**

```bash
cd backend
npm install @napi-rs/canvas fluent-ffmpeg ffmpeg-static twilio
npm install --save-dev @types/fluent-ffmpeg
```

Note : `twilio` inclut ses propres types. `ffmpeg-static` fournit le binaire ffmpeg multi-plateforme (Windows dev + Render Linux).

- [ ] **Vérifier que le build TypeScript accepte les nouveaux packages**

```bash
npm run build
```

Erreurs attendues : imports manquants dans les fichiers non encore créés → OK pour l'instant.

- [ ] **Commit**

```bash
git add package.json package-lock.json
git commit -m "chore(backend): ajout @napi-rs/canvas, fluent-ffmpeg, ffmpeg-static, twilio"
```

---

## Task 3 : schemas/client.ts — nouveaux config_types

**Files:**
- Modify: `backend/src/schemas/client.ts:3-23`

- [ ] **Ajouter 4 config_types dans ALLOWED_CONFIG_TYPES**

Remplacer le tableau `ALLOWED_CONFIG_TYPES` existant par :

```typescript
export const ALLOWED_CONFIG_TYPES = [
  'sender_name',
  'template_onboarding_j0',
  'template_onboarding_j3',
  'template_onboarding_j7',
  'template_failed_payment',
  'template_failed_payment_j1',
  'template_failed_payment_j3',
  'template_failed_payment_j7',
  'upsell_enabled',
  'upsell_product_name',
  'upsell_url',
  'upsell_price',
  'support_email_enabled',
  'support_auto_reply',
  'politique_remboursement',
  'template_checkout_abandon',
  'template_testimonial_j30',
  'template_testimonial_j60',
  'testimonial_url',
  'template_predunning',
  'template_churn_reengagement',
  'template_coaching_j14',
  'rapport_video_active',
] as const
```

- [ ] **Commit**

```bash
git add backend/src/schemas/client.ts
git commit -m "feat(backend): ajout config_types predunning, churn, coaching, rapport_video"
```

---

## Task 4 : utils/tracking.ts — paramètre channel

**Files:**
- Modify: `backend/src/utils/tracking.ts:10-27`

- [ ] **Ajouter `channel` optionnel dans insertTrackingRow**

```typescript
export async function insertTrackingRow(opts: {
  clientId: string
  studentEmail: string
  configType: string
  automationId?: string
  channel?: string
}): Promise<string> {
  const id = randomUUID()
  const { error } = await supabase.from('email_tracking').insert({
    id,
    client_id: opts.clientId,
    student_email: opts.studentEmail.toLowerCase(),
    config_type: opts.configType,
    automation_id: opts.automationId ?? null,
    channel: opts.channel ?? 'email',
    sent_at: new Date().toISOString(),
  })
  if (error) console.warn('[tracking] insert failed:', error.message)
  return id
}
```

- [ ] **Commit**

```bash
git add backend/src/utils/tracking.ts
git commit -m "feat(backend): tracking — paramètre channel optionnel (email/sms/whatsapp)"
```

---

## Task 5 : services/sms.ts

**Files:**
- Create: `backend/src/services/sms.ts`

- [ ] **Créer le service SMS Twilio**

```typescript
import twilio from 'twilio'

function getClient() {
  return twilio(process.env.TWILIO_ACCOUNT_SID!, process.env.TWILIO_AUTH_TOKEN!)
}

export async function sendSms(to: string, body: string): Promise<void> {
  const client = getClient()
  await client.messages.create({
    from: process.env.TWILIO_FROM_NUMBER!,
    to,
    body: body.slice(0, 160),
  })
}
```

- [ ] **Commit**

```bash
git add backend/src/services/sms.ts
git commit -m "feat(backend): service SMS Twilio"
```

---

## Task 6 : services/whatsapp.ts

**Files:**
- Create: `backend/src/services/whatsapp.ts`

- [ ] **Créer le service WhatsApp Meta Graph API v19.0**

```typescript
const BASE = 'https://graph.facebook.com/v19.0'

export async function validateWhatsApp(
  phoneNumberId: string,
  accessToken: string
): Promise<{ phone_number: string }> {
  const res = await fetch(`${BASE}/${phoneNumberId}?fields=display_phone_number&access_token=${accessToken}`)
  if (!res.ok) {
    const err = await res.json() as any
    throw new Error(`Meta API validation échouée: ${err?.error?.message ?? res.status}`)
  }
  const data = await res.json() as any
  return { phone_number: data.display_phone_number as string }
}

export async function sendWhatsApp(opts: {
  phoneNumberId: string
  accessToken: string
  to: string
  body: string
}): Promise<void> {
  const res = await fetch(`${BASE}/${opts.phoneNumberId}/messages`, {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${opts.accessToken}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      messaging_product: 'whatsapp',
      to: opts.to,
      type: 'text',
      text: { body: opts.body },
    }),
  })
  if (!res.ok) {
    const err = await res.json() as any
    throw new Error(`WhatsApp send failed: ${err?.error?.message ?? res.status}`)
  }
}
```

- [ ] **Commit**

```bash
git add backend/src/services/whatsapp.ts
git commit -m "feat(backend): service WhatsApp Meta Graph API v19.0"
```

---

## Task 7 : utils/sendMultiChannel.ts

**Files:**
- Create: `backend/src/utils/sendMultiChannel.ts`

Ce helper encapsule email + SMS optionnel + WhatsApp optionnel pour les auto-send. Il est injecté dans les 6 auto-send existants (Tasks 8 + 10).

- [ ] **Créer le helper**

```typescript
import { supabase } from '../services/supabase'
import { decrypt } from '../services/encryption'
import { sendEmail } from '../services/resend'
import { sendSms } from '../services/sms'
import { sendWhatsApp } from '../services/whatsapp'
import { insertTrackingRow, injectTracking } from './tracking'

function injectVars(text: string, vars: Record<string, string>): string {
  return text.replace(/\{\{(\w+)\}\}/g, (_, k) => vars[k] ?? `{{${k}}}`)
}

export interface MultiChannelOpts {
  clientId: string
  studentEmail: string
  configType: string
  automationId?: string
  /** JSON décrypté du config_type du template (ex: template_failed_payment_j1) — optionnel */
  configJson?: Record<string, any>
  /** Variables pour interpoler sms_body/whatsapp_body */
  templateVars?: Record<string, string>
  to: string
  subject: string
  /** HTML non tracké — le tracking sera injecté ici */
  rawHtml: string
  senderName: string
  replyTo?: string
}

/**
 * Envoie l'email (avec tracking) puis SMS + WhatsApp silencieusement si configurés.
 * Retourne le trackingToken de l'email.
 */
export async function sendEmailWithChannels(opts: MultiChannelOpts): Promise<string> {
  // ── Email ──
  const token = await insertTrackingRow({
    clientId: opts.clientId,
    studentEmail: opts.studentEmail,
    configType: opts.configType,
    automationId: opts.automationId,
    channel: 'email',
  })
  const trackedHtml = injectTracking(opts.rawHtml, token, process.env.BACKEND_URL!)
  await sendEmail({
    to: opts.to,
    subject: opts.subject,
    html: trackedHtml,
    sender_name: opts.senderName,
    reply_to: opts.replyTo,
  })

  // ── SMS + WhatsApp (seulement si config JSON renseigné) ──
  const cfg = opts.configJson
  if (!cfg) return token

  const hasSms = cfg.sms_active === true && typeof cfg.sms_body === 'string'
  const hasWa  = cfg.whatsapp_active === true && typeof cfg.whatsapp_body === 'string'
  if (!hasSms && !hasWa) return token

  // Lookup phone + client WA creds en parallèle
  const [profileRes, clientRes] = await Promise.all([
    supabase
      .from('student_profiles')
      .select('phone')
      .eq('client_id', opts.clientId)
      .eq('email', opts.studentEmail.toLowerCase())
      .single(),
    hasWa
      ? supabase
          .from('clients')
          .select('whatsapp_phone_number_id, whatsapp_access_token, whatsapp_active')
          .eq('id', opts.clientId)
          .single()
      : Promise.resolve({ data: null }),
  ])

  const phone = profileRes.data?.phone ?? null
  if (!phone) return token

  const vars = opts.templateVars ?? {}

  // SMS
  if (hasSms) {
    try {
      const body = injectVars(cfg.sms_body as string, vars).slice(0, 160)
      await sendSms(phone, body)
      await insertTrackingRow({
        clientId: opts.clientId,
        studentEmail: opts.studentEmail,
        configType: opts.configType,
        channel: 'sms',
      })
    } catch (e: any) {
      console.error('[sms] silenced:', e.message)
    }
  }

  // WhatsApp
  if (hasWa) {
    const cl = (clientRes as any).data
    if (cl?.whatsapp_active && cl.whatsapp_phone_number_id && cl.whatsapp_access_token) {
      try {
        const body = injectVars(cfg.whatsapp_body as string, vars)
        const accessToken = decrypt(cl.whatsapp_access_token as string)
        await sendWhatsApp({
          phoneNumberId: cl.whatsapp_phone_number_id as string,
          accessToken,
          to: phone,
          body,
        })
        await insertTrackingRow({
          clientId: opts.clientId,
          studentEmail: opts.studentEmail,
          configType: opts.configType,
          channel: 'whatsapp',
        })
      } catch (e: any) {
        console.error('[whatsapp] silenced:', e.message)
      }
    }
  }

  return token
}
```

- [ ] **Commit**

```bash
git add backend/src/utils/sendMultiChannel.ts
git commit -m "feat(backend): helper sendEmailWithChannels — email + SMS + WhatsApp multi-canal"
```

---

## Task 8 : Feature 14 — webhooks.ts (nouveaux events Stripe + extraction phone)

**Files:**
- Modify: `backend/src/routes/webhooks.ts`

Ajouter dans le handler principal (après `checkout.session.expired`) :
- `customer.updated` → `handleCardExpUpdate`
- `payment_method.updated` → `handleCardExpUpdate`
- Extraction du `phone` dans `handleCheckoutCompleted`

- [ ] **Ajouter les imports Stripe + upsert student_profiles en haut du fichier**

Après la ligne `import { insertTrackingRow, injectTracking } from '../utils/tracking'`, ajouter :

```typescript
import Stripe from 'stripe'
```

- [ ] **Ajouter les deux nouvelles branches dans le handler principal**

Dans `webhooksRouter.post('/:clientId', ...)`, après le bloc `else if (event.type === 'checkout.session.expired')` (ligne ~60), ajouter :

```typescript
  } else if (event.type === 'customer.updated') {
    await handleCardExpUpdate({ event, clientId })
  } else if (event.type === 'payment_method.updated') {
    await handleCardExpUpdate({ event, clientId })
  }
```

- [ ] **Ajouter la fonction handleCardExpUpdate**

À la fin du fichier (après `handleCheckoutSessionExpired`), ajouter :

```typescript
async function handleCardExpUpdate(opts: { event: any; clientId: string }): Promise<void> {
  const { event, clientId } = opts
  const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!)
  let email: string | null = null
  let expMonth: number | null = null
  let expYear: number | null = null

  if (event.type === 'customer.updated') {
    const customer = event.data.object as any
    email = customer.email ?? null
    const pmId = customer.invoice_settings?.default_payment_method
    if (pmId && typeof pmId === 'string') {
      try {
        const pm = await stripe.paymentMethods.retrieve(pmId)
        expMonth = pm.card?.exp_month ?? null
        expYear  = pm.card?.exp_year  ?? null
      } catch {}
    }
  } else {
    // payment_method.updated
    const pm = event.data.object as any
    expMonth = pm.card?.exp_month ?? null
    expYear  = pm.card?.exp_year  ?? null
    const customerId = typeof pm.customer === 'string' ? pm.customer : null
    if (customerId) {
      try {
        const customer = await stripe.customers.retrieve(customerId)
        if ((customer as any).deleted) return
        email = (customer as Stripe.Customer).email ?? null
      } catch {}
    }
  }

  if (!email || !expMonth || !expYear) return

  const cardExp = new Date(expYear, expMonth - 1, 1).toISOString()

  const { error } = await supabase
    .from('student_profiles')
    .upsert(
      { client_id: clientId, email: email.toLowerCase(), card_exp: cardExp, updated_at: new Date().toISOString() },
      { onConflict: 'client_id,email' }
    )
  if (error) console.error('[webhook] student_profiles upsert card_exp:', error.message)
  else console.log(`[webhook] card_exp mise à jour pour ${email} — expire ${expMonth}/${expYear}`)
}
```

- [ ] **Extraire le phone dans handleCheckoutCompleted**

Dans `handleCheckoutCompleted`, après avoir construit `context_json` (ligne ~203-212), ajouter avant le calcul des dates j3/j7 :

```typescript
  // Stocker le numéro de téléphone dans student_profiles
  const phone = session.customer_details?.phone ?? null
  if (context_json.customer_email && phone) {
    await supabase
      .from('student_profiles')
      .upsert(
        {
          client_id: clientId,
          email: (context_json.customer_email as string).toLowerCase(),
          phone,
          updated_at: new Date().toISOString(),
        },
        { onConflict: 'client_id,email' }
      )
  }
```

- [ ] **npm run build — vérifier que le fichier compile**

```bash
cd backend && npm run build 2>&1 | grep -E "error|Error"
```

Attendu : aucune erreur sur webhooks.ts.

- [ ] **Commit**

```bash
git add backend/src/routes/webhooks.ts
git commit -m "feat(backend): feature 14 — customer.updated/payment_method.updated → card_exp + phone extraction checkout"
```

---

## Task 9 : Feature 14 — cron.ts (runPredunning)

**Files:**
- Modify: `backend/src/cron.ts`

- [ ] **Ajouter les imports nécessaires en haut de cron.ts**

Après les imports existants, ajouter :

```typescript
import { getEmailTemplate } from './utils/getEmailTemplate'
```

(déjà importé — vérifier qu'il l'est)

- [ ] **Ajouter la fonction runPredunning à la fin de cron.ts**

```typescript
export async function runPredunning(): Promise<void> {
  if (new Date().getUTCHours() !== 8) return

  const now = new Date()
  const in14Days = new Date(now.getTime() + 14 * 24 * 60 * 60 * 1000).toISOString()

  const { data: profiles } = await supabase
    .from('student_profiles')
    .select('client_id, email, card_exp')
    .gte('card_exp', now.toISOString())
    .lte('card_exp', in14Days)

  if (!profiles || profiles.length === 0) return
  console.log(`[cron:predunning] ${profiles.length} profil(s) à vérifier`)

  for (const profile of profiles) {
    const { client_id: clientId, email, card_exp } = profile

    // Client pausé ?
    const { data: clientRow } = await supabase
      .from('clients')
      .select('paused_until')
      .eq('id', clientId)
      .single()
    if (clientRow?.paused_until && new Date() < new Date(clientRow.paused_until)) continue

    // Config template_predunning active ?
    const { data: configRow } = await supabase
      .from('client_configs')
      .select('encrypted_value')
      .eq('client_id', clientId)
      .eq('config_type', 'template_predunning')
      .single()
    if (!configRow?.encrypted_value) continue
    let config: Record<string, any>
    try { config = JSON.parse(decrypt(configRow.encrypted_value)) } catch { continue }
    if (config.active === false) continue

    // Idempotence : pas déjà envoyé dans les 30 derniers jours
    const since30 = new Date(now.getTime() - 30 * 24 * 60 * 60 * 1000).toISOString()
    const { count: alreadySent } = await supabase
      .from('email_tracking')
      .select('*', { count: 'exact', head: true })
      .eq('client_id', clientId)
      .eq('student_email', email.toLowerCase())
      .eq('config_type', 'template_predunning')
      .gte('sent_at', since30)
    if (alreadySent && alreadySent > 0) continue

    // Contexte élève : dernière pending_task pour ce client + email
    const { data: taskRow } = await supabase
      .from('pending_tasks')
      .select('context_json')
      .eq('client_id', clientId)
      .contains('context_json', { customer_email: email })
      .order('created_at', { ascending: false })
      .limit(1)
      .single()
    const ctx = (taskRow?.context_json as Record<string, any>) ?? {}

    // Sender name
    const { data: senderRow } = await supabase
      .from('client_configs')
      .select('encrypted_value')
      .eq('client_id', clientId)
      .eq('config_type', 'sender_name')
      .single()
    const senderName = senderRow?.encrypted_value
      ? (() => { try { return decrypt(senderRow.encrypted_value) } catch { return 'Formateur' } })()
      : 'Formateur'

    // Formater la date d'expiration (ex: "01/2026")
    const expDate = new Date(card_exp)
    const dateExpiration = `${String(expDate.getUTCMonth() + 1).padStart(2, '0')}/${expDate.getUTCFullYear()}`

    const vars: Record<string, string> = {
      nom: ctx.customer_name ?? ctx.student_name ?? '',
      prenom: ctx.student_name ?? '',
      email,
      nom_formation: ctx.product_name ?? '',
      lien_paiement: ctx.payment_link ?? ctx.hosted_invoice_url ?? '',
      date_expiration: dateExpiration,
    }

    const injectVars = (text: string) =>
      text.replace(/\{\{(\w+)\}\}/g, (_, k) => vars[k] ?? `{{${k}}}`)

    try {
      const subject = injectVars(config.subject ?? 'Votre carte bancaire expire bientôt')
      const body = injectVars(config.body ?? '')
      const html = wrapEmailHtml(body.replace(/\n/g, '<br>'), senderName)
      const token = await insertTrackingRow({ clientId, studentEmail: email, configType: 'template_predunning', channel: 'email' })
      await sendEmail({
        to: email,
        subject,
        html: injectTracking(html, token, process.env.BACKEND_URL!),
        sender_name: senderName,
      })
      await supabase.from('activity_logs').insert({
        client_id: clientId,
        action_type: 'predunning_sent',
        payload_json: { to: email, subject, date_expiration: dateExpiration, tracking_id: token },
        status: 'sent',
      })
      console.log(`[cron:predunning] → ${email} (expire ${dateExpiration})`)
    } catch (err: any) {
      console.error(`[cron:predunning] échoué pour ${email}:`, err.message)
      await supabase.from('activity_logs').insert({
        client_id: clientId,
        action_type: 'predunning_sent',
        payload_json: { to: email, error: err.message },
        status: 'failed',
      })
    }
  }
}
```

Note : les imports `insertTrackingRow` et `injectTracking` sont déjà en tête de cron.ts — supprimer le `await import(...)` et utiliser directement les imports statiques du fichier.

Note : `insertTrackingRow` et `injectTracking` sont déjà importés en haut de cron.ts (ligne 6) — les utiliser directement dans la fonction, pas de dynamic import nécessaire.

- [ ] **Commit**

```bash
git add backend/src/cron.ts
git commit -m "feat(backend): feature 14 — cron runPredunning (pré-dunning CB expirante)"
```

---

## Task 10 : Feature 15 — cron.ts (runChurnDetection)

**Files:**
- Modify: `backend/src/cron.ts`

- [ ] **Ajouter runChurnDetection à la fin de cron.ts**

```typescript
export async function runChurnDetection(): Promise<void> {
  if (new Date().getUTCHours() !== 8) return

  const now = new Date()
  const since21 = new Date(now.getTime() - 21 * 24 * 60 * 60 * 1000).toISOString()
  const since30 = new Date(now.getTime() - 30 * 24 * 60 * 60 * 1000).toISOString()
  const since7  = new Date(now.getTime() -  7 * 24 * 60 * 60 * 1000).toISOString()

  // Clients ayant un config template_churn_reengagement actif
  const { data: configs } = await supabase
    .from('client_configs')
    .select('client_id, encrypted_value')
    .eq('config_type', 'template_churn_reengagement')

  if (!configs || configs.length === 0) return

  for (const cfg of configs) {
    let config: Record<string, any>
    try { config = JSON.parse(decrypt(cfg.encrypted_value)) } catch { continue }
    if (config.active === false) continue

    const clientId = cfg.client_id

    const { data: clientRow } = await supabase
      .from('clients')
      .select('paused_until')
      .eq('id', clientId)
      .single()
    if (clientRow?.paused_until && new Date() < new Date(clientRow.paused_until)) continue

    // Élèves inscrits depuis > 7 jours (first task < since7)
    const { data: tasks } = await supabase
      .from('pending_tasks')
      .select('context_json, created_at')
      .eq('client_id', clientId)
      .lt('created_at', since7)
      .order('created_at', { ascending: true })
      .limit(2000)

    const emailsSeen = new Set<string>()
    const candidates: Array<{ email: string; ctx: Record<string, any> }> = []
    for (const t of tasks ?? []) {
      const email = (t.context_json as any)?.customer_email as string | undefined
      if (!email || emailsSeen.has(email)) continue
      emailsSeen.add(email)
      candidates.push({ email, ctx: t.context_json as Record<string, any> })
    }

    // Sender name
    const { data: senderRow } = await supabase
      .from('client_configs')
      .select('encrypted_value')
      .eq('client_id', clientId)
      .eq('config_type', 'sender_name')
      .single()
    const senderName = senderRow?.encrypted_value
      ? (() => { try { return decrypt(senderRow.encrypted_value) } catch { return 'Formateur' } })()
      : 'Formateur'

    for (const { email, ctx } of candidates) {
      // Vérifier last_lms_activity si renseigné (priorité)
      const { data: profileRow } = await supabase
        .from('student_profiles')
        .select('last_lms_activity')
        .eq('client_id', clientId)
        .eq('email', email.toLowerCase())
        .single()

      let isChurn = false

      if (profileRow?.last_lms_activity) {
        // Signal LMS dispo — inactivité > 14 jours
        isChurn = new Date(profileRow.last_lms_activity) < new Date(now.getTime() - 14 * 24 * 60 * 60 * 1000)
      } else {
        // Fallback email : aucun open 21j ET aucun click 30j
        const [openRes, clickRes] = await Promise.all([
          supabase
            .from('email_tracking')
            .select('*', { count: 'exact', head: true })
            .eq('client_id', clientId)
            .eq('student_email', email.toLowerCase())
            .not('opened_at', 'is', null)
            .gte('sent_at', since21),
          supabase
            .from('email_tracking')
            .select('*', { count: 'exact', head: true })
            .eq('client_id', clientId)
            .eq('student_email', email.toLowerCase())
            .not('clicked_at', 'is', null)
            .gte('sent_at', since30),
        ])
        isChurn = (openRes.count ?? 0) === 0 && (clickRes.count ?? 0) === 0
      }

      if (!isChurn) continue

      // Idempotence : pas d'email churn dans les 30 derniers jours
      const { count: alreadySent } = await supabase
        .from('email_tracking')
        .select('*', { count: 'exact', head: true })
        .eq('client_id', clientId)
        .eq('student_email', email.toLowerCase())
        .eq('config_type', 'template_churn_reengagement')
        .gte('sent_at', since30)
      if (alreadySent && alreadySent > 0) continue

      const vars: Record<string, string> = {
        nom: ctx.customer_name ?? ctx.student_name ?? '',
        prenom: ctx.student_name ?? '',
        nom_formation: ctx.product_name ?? '',
        lien_acces: ctx.hosted_invoice_url ?? '',
      }
      const injectV = (text: string) =>
        text.replace(/\{\{(\w+)\}\}/g, (_, k) => vars[k] ?? `{{${k}}}`)

      try {
        const subject = injectV(config.subject ?? 'On pense à vous !')
        const body = injectV(config.body ?? '')
        const html = wrapEmailHtml(body.replace(/\n/g, '<br>'), senderName)
        const token = await insertTrackingRow({ clientId, studentEmail: email, configType: 'template_churn_reengagement', channel: 'email' })
        await sendEmail({
          to: email,
          subject,
          html: injectTracking(html, token, process.env.BACKEND_URL!),
          sender_name: senderName,
        })
        await supabase.from('activity_logs').insert({
          client_id: clientId,
          action_type: 'churn_reengagement_sent',
          payload_json: { to: email, subject, tracking_id: token },
          status: 'sent',
        })
        console.log(`[cron:churn] re-engagement → ${email}`)
      } catch (err: any) {
        console.error(`[cron:churn] échoué pour ${email}:`, err.message)
        await supabase.from('activity_logs').insert({
          client_id: clientId,
          action_type: 'churn_reengagement_sent',
          payload_json: { to: email, error: err.message },
          status: 'failed',
        })
      }
    }
  }
}
```

- [ ] **Commit**

```bash
git add backend/src/cron.ts
git commit -m "feat(backend): feature 15 — cron runChurnDetection (churn prédictif)"
```

---

## Task 11 : Feature 19 — cron.ts (runStudentCoaching)

**Files:**
- Modify: `backend/src/cron.ts`

- [ ] **Ajouter runStudentCoaching à la fin de cron.ts**

```typescript
export async function runStudentCoaching(): Promise<void> {
  if (new Date().getUTCHours() !== 8) return

  const now = new Date()
  const since14 = new Date(now.getTime() - 14 * 24 * 60 * 60 * 1000).toISOString()
  const since7  = new Date(now.getTime() -  7 * 24 * 60 * 60 * 1000).toISOString()

  const { data: configs } = await supabase
    .from('client_configs')
    .select('client_id, encrypted_value')
    .eq('config_type', 'template_coaching_j14')

  if (!configs || configs.length === 0) return

  for (const cfg of configs) {
    let config: Record<string, any>
    try { config = JSON.parse(decrypt(cfg.encrypted_value)) } catch { continue }
    if (config.active === false) continue

    const clientId = cfg.client_id

    const { data: clientRow } = await supabase
      .from('clients')
      .select('paused_until')
      .eq('id', clientId)
      .single()
    if (clientRow?.paused_until && new Date() < new Date(clientRow.paused_until)) continue

    // Élèves inscrits depuis > 7 jours
    const { data: tasks } = await supabase
      .from('pending_tasks')
      .select('context_json, created_at')
      .eq('client_id', clientId)
      .lt('created_at', since7)
      .order('created_at', { ascending: true })
      .limit(2000)

    const emailsSeen = new Set<string>()
    const candidates: Array<{ email: string; ctx: Record<string, any> }> = []
    for (const t of tasks ?? []) {
      const email = (t.context_json as any)?.customer_email as string | undefined
      if (!email || emailsSeen.has(email)) continue
      emailsSeen.add(email)
      candidates.push({ email, ctx: t.context_json as Record<string, any> })
    }

    const { data: senderRow } = await supabase
      .from('client_configs')
      .select('encrypted_value')
      .eq('client_id', clientId)
      .eq('config_type', 'sender_name')
      .single()
    const senderName = senderRow?.encrypted_value
      ? (() => { try { return decrypt(senderRow.encrypted_value) } catch { return 'Formateur' } })()
      : 'Formateur'

    for (const { email, ctx } of candidates) {
      const { data: profileRow } = await supabase
        .from('student_profiles')
        .select('last_lms_activity')
        .eq('client_id', clientId)
        .eq('email', email.toLowerCase())
        .single()

      let needsCoaching = false

      if (profileRow?.last_lms_activity) {
        // LMS dispo — inactivité > 7 jours
        needsCoaching = new Date(profileRow.last_lms_activity) < new Date(now.getTime() - 7 * 24 * 60 * 60 * 1000)
      } else {
        // Fallback email — aucun open depuis 14 jours
        const { count: opens } = await supabase
          .from('email_tracking')
          .select('*', { count: 'exact', head: true })
          .eq('client_id', clientId)
          .eq('student_email', email.toLowerCase())
          .not('opened_at', 'is', null)
          .gte('sent_at', since14)
        needsCoaching = (opens ?? 0) === 0
      }

      if (!needsCoaching) continue

      // Idempotence : pas de coaching dans les 14 derniers jours
      const { count: alreadySent } = await supabase
        .from('email_tracking')
        .select('*', { count: 'exact', head: true })
        .eq('client_id', clientId)
        .eq('student_email', email.toLowerCase())
        .eq('config_type', 'template_coaching_j14')
        .gte('sent_at', since14)
      if (alreadySent && alreadySent > 0) continue

      const vars: Record<string, string> = {
        nom: ctx.customer_name ?? ctx.student_name ?? '',
        prenom: ctx.student_name ?? '',
        nom_formation: ctx.product_name ?? '',
        lien_acces: ctx.hosted_invoice_url ?? '',
      }
      const injectV = (text: string) =>
        text.replace(/\{\{(\w+)\}\}/g, (_, k) => vars[k] ?? `{{${k}}}`)

      try {
        const subject = injectV(config.subject ?? 'Comment avancez-vous dans votre formation ?')
        const body = injectV(config.body ?? '')
        const html = wrapEmailHtml(body.replace(/\n/g, '<br>'), senderName)
        const token = await insertTrackingRow({ clientId, studentEmail: email, configType: 'template_coaching_j14', channel: 'email' })
        await sendEmail({
          to: email,
          subject,
          html: injectTracking(html, token, process.env.BACKEND_URL!),
          sender_name: senderName,
        })
        await supabase.from('activity_logs').insert({
          client_id: clientId,
          action_type: 'coaching_sent',
          payload_json: { to: email, subject, tracking_id: token },
          status: 'sent',
        })
        console.log(`[cron:coaching] → ${email}`)
      } catch (err: any) {
        console.error(`[cron:coaching] échoué pour ${email}:`, err.message)
        await supabase.from('activity_logs').insert({
          client_id: clientId,
          action_type: 'coaching_sent',
          payload_json: { to: email, error: err.message },
          status: 'failed',
        })
      }
    }
  }
}
```

- [ ] **Commit**

```bash
git add backend/src/cron.ts
git commit -m "feat(backend): feature 19 — cron runStudentCoaching (coaching pédagogique J14)"
```

---

## Task 12 : Feature 20+16 — webhooks.ts auto-send (injection sendEmailWithChannels)

**Files:**
- Modify: `backend/src/routes/webhooks.ts`

Remplacer dans les auto-send de `handleFailedPayment` et `handleCheckoutCompleted` les appels `insertTrackingRow` + `sendEmail` par `sendEmailWithChannels`.

- [ ] **Ajouter l'import sendEmailWithChannels en haut de webhooks.ts**

```typescript
import { sendEmailWithChannels } from '../utils/sendMultiChannel'
```

- [ ] **Modifier handleFailedPayment (bloc auto-mode, ~ligne 157-188)**

Remplacer :
```typescript
    if (!context_json.customer_email) throw new Error('customer_email manquant')
    const aiResponse = await callClaude(prompt_template, 'claude-sonnet-4-6')
    const { subject, body_html } = parseClaudeResponse(aiResponse)
    const html = wrapEmailHtml(body_html, sender_name)
    const trackingToken = await insertTrackingRow({
      clientId,
      studentEmail: context_json.customer_email,
      configType: 'template_failed_payment_j1',
    })
    await sendEmail({ to: context_json.customer_email, subject, html: injectTracking(html, trackingToken, process.env.BACKEND_URL!), sender_name, reply_to: client?.email })
```

Par :
```typescript
    if (!context_json.customer_email) throw new Error('customer_email manquant')
    const aiResponse = await callClaude(prompt_template, 'claude-sonnet-4-6')
    const { subject, body_html } = parseClaudeResponse(aiResponse)
    const rawHtml = wrapEmailHtml(body_html, sender_name)
    let configJson: Record<string, any> | undefined
    try { configJson = configMap['template_failed_payment_j1'] ? JSON.parse(configMap['template_failed_payment_j1']) : undefined } catch {}
    const tplVarsW = {
      nom: context_json.customer_name ?? context_json.student_name ?? '',
      prenom: context_json.student_name ?? '',
      email: context_json.customer_email,
      nom_formation: context_json.product_name ?? '',
      lien_paiement: context_json.payment_link ?? context_json.hosted_invoice_url ?? '',
    }
    const trackingToken = await sendEmailWithChannels({
      clientId,
      studentEmail: context_json.customer_email,
      configType: 'template_failed_payment_j1',
      configJson,
      templateVars: tplVarsW,
      to: context_json.customer_email,
      subject,
      rawHtml,
      senderName: sender_name,
      replyTo: client?.email,
    })
```

- [ ] **Modifier handleCheckoutCompleted (bloc auto-mode, ~ligne 269-281)**

Remplacer :
```typescript
    if (!context_json.customer_email) throw new Error('customer_email manquant')
    const aiResponse = await callClaude(prompt, 'claude-sonnet-4-6')
    const { subject, body_html } = parseClaudeResponse(aiResponse)
    const html = wrapEmailHtml(body_html, sender_name)
    const trackingToken = await insertTrackingRow({
      clientId,
      studentEmail: context_json.customer_email,
      configType: 'template_onboarding_j0',
    })
    await sendEmail({ to: context_json.customer_email, subject, html: injectTracking(html, trackingToken, process.env.BACKEND_URL!), sender_name, reply_to: client?.email })
```

Par :
```typescript
    if (!context_json.customer_email) throw new Error('customer_email manquant')
    const aiResponse = await callClaude(prompt, 'claude-sonnet-4-6')
    const { subject, body_html } = parseClaudeResponse(aiResponse)
    const rawHtml = wrapEmailHtml(body_html, sender_name)
    let j0ConfigJson: Record<string, any> | undefined
    try { j0ConfigJson = configMap['template_onboarding_j0'] ? JSON.parse(configMap['template_onboarding_j0']) : undefined } catch {}
    const tplVarsO = {
      nom: context_json.customer_name ?? context_json.student_name ?? '',
      prenom: context_json.student_name ?? '',
      email: context_json.customer_email,
      nom_formation: context_json.product_name ?? '',
      lien_acces: '',
    }
    const trackingToken = await sendEmailWithChannels({
      clientId,
      studentEmail: context_json.customer_email,
      configType: 'template_onboarding_j0',
      configJson: j0ConfigJson,
      templateVars: tplVarsO,
      to: context_json.customer_email,
      subject,
      rawHtml,
      senderName: sender_name,
      replyTo: client?.email,
    })
```

- [ ] **Supprimer les imports inutilisés dans webhooks.ts si insertTrackingRow/injectTracking ne sont plus utilisés directement**

Vérifier que `insertTrackingRow` et `injectTracking` sont encore utilisés dans le fichier (handlePaymentRecovered, handleCheckoutSessionExpired n'en ont pas besoin, mais ils pourraient être dans d'autres parties). Supprimer les imports inutilisés si nécessaire.

- [ ] **npm run build**

```bash
cd backend && npm run build 2>&1 | grep -E "error TS"
```

Attendu : aucune erreur.

- [ ] **Commit**

```bash
git add backend/src/routes/webhooks.ts
git commit -m "feat(backend): feature 20+16 — webhooks auto-send via sendEmailWithChannels"
```

---

## Task 13 : Feature 20+16 — cron.ts auto-send (injection sendEmailWithChannels)

**Files:**
- Modify: `backend/src/cron.ts`

Injecter `sendEmailWithChannels` dans `handleUpsellJob`, `handleCheckoutAbandonJob`, et `runCustomAutomations`.

- [ ] **Ajouter l'import sendEmailWithChannels en haut de cron.ts**

```typescript
import { sendEmailWithChannels } from './utils/sendMultiChannel'
```

- [ ] **Modifier handleUpsellJob (auto-mode, ~ligne 170-196)**

Remplacer :
```typescript
    const html = wrapEmailHtml(body_html, sender_name)
    const trackingToken = await insertTrackingRow({
      clientId: job.client_id,
      studentEmail: ctx.customer_email,
      configType: 'upsell',
    })
    await sendEmail({ to: ctx.customer_email, subject, html: injectTracking(html, trackingToken, process.env.BACKEND_URL!), sender_name: ctx.sender_name ?? 'Formateur', reply_to: (client as any)?.email })
```

Par :
```typescript
    const rawHtml = wrapEmailHtml(body_html, sender_name)
    const trackingToken = await sendEmailWithChannels({
      clientId: job.client_id,
      studentEmail: ctx.customer_email,
      configType: 'upsell',
      to: ctx.customer_email,
      subject,
      rawHtml,
      senderName: ctx.sender_name ?? 'Formateur',
      replyTo: (client as any)?.email,
    })
```

- [ ] **Modifier handleCheckoutAbandonJob (~ligne 260-270)**

Remplacer :
```typescript
    await sendEmail({ to: customerEmail, subject, html: injectTracking(html, trackingToken, process.env.BACKEND_URL!), sender_name: senderName })
    await markDone()
```

Par :
```typescript
    let abandonConfigJson: Record<string, any> | undefined
    try {
      const { data: abandonRow } = await supabase.from('client_configs').select('encrypted_value').eq('client_id', job.client_id).eq('config_type', 'template_checkout_abandon').single()
      if (abandonRow?.encrypted_value) abandonConfigJson = JSON.parse(decrypt(abandonRow.encrypted_value))
    } catch {}
    const trackingToken = await sendEmailWithChannels({
      clientId: job.client_id,
      studentEmail: customerEmail,
      configType: 'template_checkout_abandon',
      configJson: abandonConfigJson,
      templateVars: vars,
      to: customerEmail,
      subject,
      rawHtml: html,
      senderName,
    })
    await markDone()
```

Note : dans `handleCheckoutAbandonJob`, `html` est déjà `wrapEmailHtml(body.replace(...), senderName)` — c'est le `rawHtml` à passer. Supprimer le `insertTrackingRow` et `injectTracking(html, trackingToken, ...)` existants puisque `sendEmailWithChannels` les gère.

- [ ] **Modifier runCustomAutomations (~ligne 362-370)**

Remplacer :
```typescript
      const html = wrapEmailHtml(automation.body.replace(/\n/g, '<br>'), senderName)
      const trackingToken = await insertTrackingRow({
        clientId: automation.client_id,
        studentEmail: client.email,
        configType: 'custom_automation',
        automationId: automation.id,
      })
      await sendEmail({ to: client.email, subject: automation.subject, html: injectTracking(html, trackingToken, process.env.BACKEND_URL!), sender_name: senderName })
```

Par :
```typescript
      const rawHtml = wrapEmailHtml(automation.body.replace(/\n/g, '<br>'), senderName)
      const trackingToken = await sendEmailWithChannels({
        clientId: automation.client_id,
        studentEmail: client.email,
        configType: 'custom_automation',
        automationId: automation.id,
        to: client.email,
        subject: automation.subject,
        rawHtml,
        senderName,
      })
```

- [ ] **Vérifier que insertTrackingRow et injectTracking sont encore utilisés dans cron.ts**

Ils restent utilisés dans `runPredunning`, `runChurnDetection`, `runStudentCoaching`. Garder les imports.

- [ ] **npm run build**

```bash
cd backend && npm run build 2>&1 | grep -E "error TS"
```

Attendu : aucune erreur.

- [ ] **Commit**

```bash
git add backend/src/cron.ts
git commit -m "feat(backend): feature 20+16 — cron auto-send via sendEmailWithChannels"
```

---

## Task 14 : Feature 16 — clientAuth.ts (WhatsApp endpoints + /client/me)

**Files:**
- Modify: `backend/src/routes/clientAuth.ts`

- [ ] **Ajouter l'import whatsapp en haut de clientAuth.ts**

```typescript
import { validateWhatsApp } from '../services/whatsapp'
```

- [ ] **Modifier GET /client/me pour inclure whatsapp_connected**

Changer le `.select` ligne ~185 :

```typescript
  const { data, error } = await supabase
    .from('clients')
    .select('client_email, must_change_password, created_at, paused_until, whatsapp_active')
    .eq('id', clientId)
    .single()
```

Changer la réponse :

```typescript
  res.json({
    email: data.client_email,
    mustChangePassword: data.must_change_password,
    createdAt: data.created_at,
    pausedUntil: data.paused_until ?? null,
    whatsappConnected: data.whatsapp_active ?? false,
  })
```

- [ ] **Ajouter POST /client/settings/whatsapp (après PUT /client/settings/email)**

```typescript
// POST /client/settings/whatsapp
clientAuthRouter.post('/settings/whatsapp', authenticateClient, async (req, res) => {
  const clientId = (req as any).clientId as string
  const { phone_number_id, access_token } = req.body ?? {}

  if (!phone_number_id || !access_token) {
    return res.status(400).json({ error: 'phone_number_id et access_token requis' })
  }

  try {
    const { phone_number } = await validateWhatsApp(phone_number_id as string, access_token as string)

    const { error } = await supabase
      .from('clients')
      .update({
        whatsapp_phone_number_id: phone_number_id,
        whatsapp_access_token: encrypt(access_token as string),
        whatsapp_active: true,
      })
      .eq('id', clientId)

    if (error) return res.status(500).json({ error: error.message })

    res.json({ success: true, phone_number })
  } catch (err: any) {
    res.status(400).json({ error: err.message })
  }
})

// DELETE /client/settings/whatsapp
clientAuthRouter.delete('/settings/whatsapp', authenticateClient, async (req, res) => {
  const clientId = (req as any).clientId as string

  const { error } = await supabase
    .from('clients')
    .update({
      whatsapp_phone_number_id: null,
      whatsapp_access_token: null,
      whatsapp_active: false,
    })
    .eq('id', clientId)

  if (error) return res.status(500).json({ error: error.message })
  res.json({ ok: true })
})
```

- [ ] **npm run build**

```bash
cd backend && npm run build 2>&1 | grep -E "error TS"
```

- [ ] **Commit**

```bash
git add backend/src/routes/clientAuth.ts
git commit -m "feat(backend): feature 16 — endpoints WhatsApp + whatsapp_connected dans /client/me"
```

---

## Task 15 : Feature 17 — services/videoreport.ts

**Files:**
- Create: `backend/src/services/videoreport.ts`

Ce service génère une vidéo MP4 hebdomadaire (slides PNG + voix ElevenLabs via fetch + ffmpeg) et l'uploade dans Supabase Storage.

- [ ] **Créer backend/src/services/videoreport.ts**

```typescript
import { createCanvas } from '@napi-rs/canvas'
import ffmpeg from 'fluent-ffmpeg'
import ffmpegPath from 'ffmpeg-static'
import fs from 'fs'
import path from 'path'
import os from 'os'
import { randomUUID } from 'crypto'
import { supabase } from './supabase'

if (ffmpegPath) ffmpeg.setFfmpegPath(ffmpegPath)

const ELEVENLABS_VOICE_ID = 'EXAVITQu4vr4xnSDxMaL' // voix Sarah (française)
const SLIDE_DURATION = 7 // secondes par slide

export interface WeeklyStats {
  clientName: string
  newStudents: number
  emailsSent: number
  recovered: number
  recoveredAmount: number
  weekLabel: string // ex: "Semaine du 26 mai 2026"
}

export async function generateWeeklyVideo(
  clientId: string,
  stats: WeeklyStats
): Promise<string> {
  const tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'report-'))
  const audioPath   = path.join(tmpDir, 'voice.mp3')
  const outputPath  = path.join(tmpDir, 'report.mp4')
  const storagePath = `${clientId}/rapport-${randomUUID()}.mp4`

  try {
    // 1. Script texte
    const script = buildScript(stats)

    // 2. Audio ElevenLabs
    await generateAudio(script, audioPath)

    // 3. Slides PNG
    const slidePaths = await generateSlides(stats, tmpDir)

    // 4. Assembler MP4 avec ffmpeg
    await assembleMp4(slidePaths, audioPath, outputPath)

    // 5. Upload Supabase Storage
    const fileBuffer = fs.readFileSync(outputPath)
    const { error: uploadError } = await supabase.storage
      .from('rapports-video')
      .upload(storagePath, fileBuffer, { contentType: 'video/mp4', upsert: true })

    if (uploadError) throw new Error(`Upload échoué: ${uploadError.message}`)

    // 6. URL signée 7 jours
    const { data: signedData } = await supabase.storage
      .from('rapports-video')
      .createSignedUrl(storagePath, 7 * 24 * 3600)

    return signedData?.signedUrl ?? ''
  } finally {
    fs.rmSync(tmpDir, { recursive: true, force: true })
  }
}

function buildScript(stats: WeeklyStats): string {
  return (
    `Bonjour ${stats.clientName}. Voici votre rapport de la semaine. ` +
    `Cette semaine, vous avez accueilli ${stats.newStudents} nouvel${stats.newStudents > 1 ? 's' : ''} élève${stats.newStudents > 1 ? 's' : ''}. ` +
    `${stats.emailsSent} emails ont été envoyés automatiquement. ` +
    (stats.recovered > 0
      ? `Vous avez récupéré ${stats.recovered} paiement${stats.recovered > 1 ? 's' : ''} pour un total de ${stats.recoveredAmount.toFixed(2)} euros. `
      : '') +
    `Continuez sur cette lancée. À la semaine prochaine !`
  )
}

async function generateAudio(text: string, outputPath: string): Promise<void> {
  const apiKey = process.env.ELEVENLABS_API_KEY
  if (!apiKey) throw new Error('ELEVENLABS_API_KEY manquant')

  const res = await fetch(`https://api.elevenlabs.io/v1/text-to-speech/${ELEVENLABS_VOICE_ID}`, {
    method: 'POST',
    headers: {
      'xi-api-key': apiKey,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      text,
      model_id: 'eleven_multilingual_v2',
      voice_settings: { stability: 0.5, similarity_boost: 0.75 },
    }),
  })

  if (!res.ok) {
    const err = await res.text()
    throw new Error(`ElevenLabs erreur ${res.status}: ${err}`)
  }

  const buffer = Buffer.from(await res.arrayBuffer())
  fs.writeFileSync(outputPath, buffer)
}

async function generateSlides(stats: WeeklyStats, tmpDir: string): Promise<string[]> {
  const W = 1280, H = 720
  const slides: string[] = []

  const slideData = [
    { title: stats.weekLabel, subtitle: stats.clientName, color: '#1a1a2e' },
    { title: `${stats.newStudents}`, subtitle: 'Nouveaux élèves', color: '#16213e' },
    { title: `${stats.emailsSent}`, subtitle: 'Emails envoyés', color: '#0f3460' },
    {
      title: stats.recovered > 0 ? `${stats.recoveredAmount.toFixed(2)} €` : '—',
      subtitle: stats.recovered > 0 ? `${stats.recovered} paiement(s) récupéré(s)` : 'Aucun impayé récupéré',
      color: '#533483',
    },
  ]

  for (let i = 0; i < slideData.length; i++) {
    const { title, subtitle, color } = slideData[i]
    const canvas = createCanvas(W, H)
    const ctx    = canvas.getContext('2d')

    ctx.fillStyle = color
    ctx.fillRect(0, 0, W, H)

    ctx.fillStyle = '#ffffff'
    ctx.font = 'bold 80px sans-serif'
    ctx.textAlign = 'center'
    ctx.fillText(title, W / 2, H / 2 - 20)

    ctx.font = '36px sans-serif'
    ctx.fillStyle = '#cccccc'
    ctx.fillText(subtitle, W / 2, H / 2 + 60)

    ctx.font = '24px sans-serif'
    ctx.fillStyle = '#888888'
    ctx.fillText('AutomatePro', W / 2, H - 40)

    const slidePath = path.join(tmpDir, `slide_${i}.png`)
    fs.writeFileSync(slidePath, canvas.toBuffer('image/png'))
    slides.push(slidePath)
  }

  return slides
}

function assembleMp4(
  slidePaths: string[],
  audioPath: string,
  outputPath: string
): Promise<void> {
  return new Promise((resolve, reject) => {
    const listPath = outputPath.replace('.mp4', '.txt')
    // Répéter la dernière image pour éviter le glitch de fin ffmpeg concat
    const listContent =
      slidePaths.map(p => `file '${p}'\nduration ${SLIDE_DURATION}`).join('\n') +
      `\nfile '${slidePaths[slidePaths.length - 1]}'`
    fs.writeFileSync(listPath, listContent)

    ffmpeg()
      .input(listPath)
      .inputOptions(['-f', 'concat', '-safe', '0'])
      .input(audioPath)
      .videoCodec('libx264')
      .audioCodec('aac')
      .outputOptions(['-pix_fmt', 'yuv420p', '-shortest'])
      .output(outputPath)
      .on('end', () => resolve())
      .on('error', (err: Error) => reject(err))
      .run()
  })
}
```

- [ ] **npm run build — vérifier que videoreport.ts compile**

```bash
cd backend && npm run build 2>&1 | grep -E "error TS"
```

Si erreur sur `@napi-rs/canvas` ou `fluent-ffmpeg` : vérifier que les types sont bien installés.

- [ ] **Commit**

```bash
git add backend/src/services/videoreport.ts
git commit -m "feat(backend): feature 17 — service videoreport (ElevenLabs + canvas + ffmpeg + Supabase Storage)"
```

---

## Task 16 : Feature 17 — cron.ts (sendVideoReport)

**Files:**
- Modify: `backend/src/cron.ts`

- [ ] **Ajouter l'import generateWeeklyVideo en haut de cron.ts**

```typescript
import { generateWeeklyVideo, WeeklyStats } from './services/videoreport'
```

- [ ] **Ajouter sendVideoReport à la fin de cron.ts**

```typescript
export async function sendVideoReport(): Promise<void> {
  const now = new Date()
  if (now.getUTCDay() !== 1 || now.getUTCHours() !== 8) return

  const weekAgo = new Date(now.getTime() - 7 * 24 * 60 * 60 * 1000).toISOString()

  // Clients avec rapport_video_active = true
  const { data: videoConfigs } = await supabase
    .from('client_configs')
    .select('client_id, encrypted_value')
    .eq('config_type', 'rapport_video_active')

  const activeClientIds = new Set<string>()
  for (const cfg of videoConfigs ?? []) {
    try {
      const val = decrypt(cfg.encrypted_value)
      if (val === 'true' || JSON.parse(val) === true) activeClientIds.add(cfg.client_id)
    } catch {}
  }

  if (activeClientIds.size === 0) return
  console.log(`[cron:video] ${activeClientIds.size} client(s) avec rapport vidéo actif`)

  const { data: clients } = await supabase
    .from('clients')
    .select('id, name, email, paused_until')
    .in('id', [...activeClientIds])

  for (const client of clients ?? []) {
    if ((client as any).paused_until && new Date() < new Date((client as any).paused_until)) continue

    try {
      const { data: logs } = await supabase
        .from('activity_logs')
        .select('action_type, payload_json')
        .eq('client_id', client.id)
        .eq('status', 'sent')
        .gte('created_at', weekAgo)

      const newStudents = (logs ?? []).filter((l: any) => l.action_type === 'onboarding_j0_email').length
      const emailsSent = (logs ?? []).length
      const recoveredLogs = (logs ?? []).filter((l: any) => l.action_type === 'payment_recovered')
      const recovered = recoveredLogs.length
      const recoveredAmount = recoveredLogs.reduce((sum: number, l: any) => sum + ((l.payload_json as any)?.amount ?? 0), 0)

      const weekLabel = `Semaine du ${new Date(weekAgo).toLocaleDateString('fr-FR', { day: 'numeric', month: 'long', year: 'numeric' })}`

      const stats: WeeklyStats = {
        clientName: client.name,
        newStudents,
        emailsSent,
        recovered,
        recoveredAmount,
        weekLabel,
      }

      const videoUrl = await generateWeeklyVideo(client.id, stats)
      if (!videoUrl) throw new Error('URL vidéo vide')

      await sendEmail({
        to: client.email,
        subject: `Votre rapport vidéo — ${weekLabel}`,
        html: `<p>Bonjour ${client.name},</p><p>Votre rapport vidéo de la semaine est prêt :</p><p><a href="${videoUrl}">Voir le rapport</a></p><p>Ce lien expire dans 7 jours.</p><p>AutomatePro</p>`,
        sender_name: 'AutomatePro',
      })

      await supabase.from('activity_logs').insert({
        client_id: client.id,
        action_type: 'rapport_video_sent',
        payload_json: { video_url: videoUrl, week: weekLabel },
        status: 'sent',
      })

      console.log(`[cron:video] rapport envoyé à ${client.email}`)
    } catch (err: any) {
      console.error(`[cron:video] échoué pour ${client.name}:`, err.message)
      await supabase.from('activity_logs').insert({
        client_id: client.id,
        action_type: 'rapport_video_sent',
        payload_json: { error: err.message },
        status: 'failed',
      })
    }
  }
}
```

- [ ] **Commit**

```bash
git add backend/src/cron.ts
git commit -m "feat(backend): feature 17 — cron sendVideoReport (rapport vidéo hebdo lundi 08h)"
```

---

## Task 17 : index.ts — registration des crons + variables d'env

**Files:**
- Modify: `backend/src/index.ts`

- [ ] **Mettre à jour les imports cron en haut du fichier**

Remplacer la ligne :
```typescript
import { runScheduledJobs, runCustomAutomations, runTestimonialEmails } from './cron'
```

Par :
```typescript
import {
  runScheduledJobs,
  runCustomAutomations,
  runTestimonialEmails,
  runPredunning,
  runChurnDetection,
  runStudentCoaching,
  sendVideoReport,
} from './cron'
```

- [ ] **Enregistrer les nouvelles fonctions cron dans le bloc ENABLE_CRON**

Remplacer le bloc :
```typescript
  if (process.env.ENABLE_CRON === 'true') {
    runScheduledJobs()
    runCustomAutomations()
    runTestimonialEmails()
    setInterval(runScheduledJobs, 60 * 60 * 1000)
    setInterval(runCustomAutomations, 60 * 60 * 1000)
    setInterval(runTestimonialEmails, 60 * 60 * 1000)
  }
```

Par :
```typescript
  if (process.env.ENABLE_CRON === 'true') {
    runScheduledJobs()
    runCustomAutomations()
    runTestimonialEmails()
    runPredunning()
    runChurnDetection()
    runStudentCoaching()
    sendVideoReport()
    setInterval(runScheduledJobs,      60 * 60 * 1000)
    setInterval(runCustomAutomations,  60 * 60 * 1000)
    setInterval(runTestimonialEmails,  60 * 60 * 1000)
    setInterval(runPredunning,         60 * 60 * 1000)
    setInterval(runChurnDetection,     60 * 60 * 1000)
    setInterval(runStudentCoaching,    60 * 60 * 1000)
    setInterval(sendVideoReport,       60 * 60 * 1000)
  }
```

Note : chaque cron hourly a un time gate interne (08h UTC), donc un setInterval horaire est correct.

- [ ] **Ajouter une note sur les variables d'env optionnelles**

Les variables suivantes sont **optionnelles** (pas dans REQUIRED_ENV) — les features qui les utilisent dégradent silencieusement si absentes :
- `ELEVENLABS_API_KEY` — Feature 17 (vidéo). Ajouter un warning au démarrage si absente.
- `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_FROM_NUMBER` — Feature 20 (SMS). Idem.

Ajouter après le bloc `if (missing.length)` :

```typescript
const OPTIONAL_ENV_GROUPS = [
  { keys: ['ELEVENLABS_API_KEY'], feature: 'Feature 17 (rapport vidéo)' },
  { keys: ['TWILIO_ACCOUNT_SID', 'TWILIO_AUTH_TOKEN', 'TWILIO_FROM_NUMBER'], feature: 'Feature 20 (SMS)' },
]
for (const { keys, feature } of OPTIONAL_ENV_GROUPS) {
  if (keys.some(k => !process.env[k])) {
    console.warn(`[config] ${feature} désactivée — variables manquantes: ${keys.filter(k => !process.env[k]).join(', ')}`)
  }
}
```

- [ ] **npm run build — build complet**

```bash
cd backend && npm run build 2>&1 | grep -E "error TS"
```

Attendu : 0 erreur TypeScript.

- [ ] **Commit**

```bash
git add backend/src/index.ts
git commit -m "feat(backend): index.ts — registration des 4 nouveaux crons + warnings env optionnels"
```

---

## Task 18 : Vérification finale

- [ ] **Build TypeScript propre**

```bash
cd backend && npm run build
```

Attendu : `Found 0 errors. Watching for file changes.` (ou équivalent tsc, pas de watch).

- [ ] **Vérifier les checklist feature par feature**

Feature 14 — Prédunning :
- `student_profiles` contient `card_exp` ✓
- `runPredunning` a un time gate 08h ✓
- Idempotence 30j via `email_tracking` ✓
- Log `predunning_sent` dans `activity_logs` ✓
- Pas de renvoi si `active: false` dans le config ✓

Feature 15 — Churn :
- Critères : 21j sans open + 30j sans click + inscrit > 7j ✓
- Signal LMS prioritaire si `last_lms_activity` renseigné ✓
- Idempotence 30j ✓
- Log `churn_reengagement_sent` ✓

Feature 16 — WhatsApp :
- Credentials chiffrés (AES-256) en base ✓
- Validation via Meta API avant sauvegarde ✓
- Échec WhatsApp silencieux (ne bloque pas l'email) ✓
- `whatsapp_connected` dans `/client/me` ✓

Feature 17 — Vidéo :
- Lundi 08h UTC uniquement ✓
- Bucket `rapports-video` à créer manuellement dans Supabase Storage ✓
- URL signée 7j ✓
- Log `rapport_video_sent` avec URL ✓
- Si `ELEVENLABS_API_KEY` absent → erreur catchée → log `failed` ✓

Feature 19 — Coaching :
- Critère : 14j sans open + inscrit > 7j ✓
- Signal LMS prioritaire (7j inactivité) ✓
- Idempotence 14j ✓
- Log `coaching_sent` ✓

Feature 20 — SMS :
- Phone stocké dans `student_profiles` via checkout webhook ✓
- `sms_active` + `sms_body` dans le config JSON du template ✓
- Échec SMS silencieux ✓
- Tracking `channel: 'sms'` dans `email_tracking` ✓

- [ ] **Commit final**

```bash
git add -A
git commit -m "feat(backend): phase 3 — features 14/15/16/17/19/20 complètes"
```

---

## Variables d'environnement à ajouter

### Backend `.env`
```
# Feature 17 — Rapport vidéo
ELEVENLABS_API_KEY=your_elevenlabs_api_key

# Feature 20 — SMS Twilio
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_FROM_NUMBER=+33XXXXXXXXX
```

## Migrations à appliquer dans Supabase SQL Editor (dans l'ordre)

1. `017_student_profiles.sql`
2. `018_whatsapp_fields.sql`
3. `019_email_tracking_channel.sql`

Ensuite, créer le bucket `rapports-video` dans Supabase Storage (accès private).

## Config_types JSON attendus par les nouvelles features

**template_predunning** :
```json
{
  "active": true,
  "subject": "Votre carte bancaire expire le {{date_expiration}}",
  "body": "Bonjour {{prenom}},\n\nVotre carte bancaire utilisée pour {{nom_formation}} expire en {{date_expiration}}.\n\nMettez-la à jour ici : {{lien_paiement}}"
}
```

**template_churn_reengagement** :
```json
{
  "active": true,
  "subject": "On pense à vous, {{prenom}} !",
  "body": "Bonjour {{prenom}},\n\nVotre formation {{nom_formation}} vous attend.\n\nRejoignez votre espace ici : {{lien_acces}}"
}
```

**template_coaching_j14** :
```json
{
  "active": true,
  "subject": "Comment avancez-vous dans {{nom_formation}} ?",
  "body": "Bonjour {{prenom}},\n\nVous avez rejoint {{nom_formation}} il y a quelques jours. Comment se passe votre progression ?\n\nAccédez à votre espace ici : {{lien_acces}}"
}
```

**SMS dans un template existant (ex : template_failed_payment_j1)** :
```json
{
  "active": true,
  "subject": "...",
  "body": "...",
  "sms_active": true,
  "sms_body": "Bonjour {{prenom}}, votre paiement pour {{nom_formation}} a échoué. Régularisez ici : {{lien_paiement}}"
}
```

**WhatsApp dans un template** :
```json
{
  "active": true,
  "subject": "...",
  "body": "...",
  "whatsapp_active": true,
  "whatsapp_body": "Bonjour {{prenom}} ! Votre paiement pour {{nom_formation}} a échoué. Cliquez ici pour régler : {{lien_paiement}}"
}
```
